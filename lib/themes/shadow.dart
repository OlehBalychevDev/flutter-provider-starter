import 'package:flutter/material.dart';
import 'package:flutter_provider_starter/themes/theme.dart';

const bottomTextShadow = Shadow(
  offset: Offset(0, 4),
  blurRadius: 4.0,
  // Color.fromARGB(0.25, 0, 0, 0)
  color: black25,
);